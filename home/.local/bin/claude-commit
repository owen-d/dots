#!/usr/bin/env bash
# Generate commit messages using Claude CLI
# Reads prompt from stdin, outputs commit message to stdout

set -euo pipefail

prompt=$(cat)
claude -p --model haiku --system-prompt "You are a helpful commit message generator, but you are a ghost writer. Do NOT include yourself as a commit co-author." "$prompt"
